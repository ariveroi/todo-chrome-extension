export const addContainerStyle = (element) => {
  element.setAttribute(
    "style",
    "background-color: white; height: 100px; width: 100px; position: fixed; top: 0"
  );
};

const addFormStyle = (element) => {};

const addButtonStyle = (element) => {};

const addInputStyle = (element) => {};
